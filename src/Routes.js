import React from "react";
import { Routes, Route } from 'react-router-dom';

export default () => {
    return (
        <Routes>
            <Route path="/">
                Pagina Inicial
            </Route>
        </Routes>
    );
}
